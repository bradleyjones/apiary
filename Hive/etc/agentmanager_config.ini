[Base]

[Rabbit]
    host = 127.0.0.1
    rpc_queue = control
    sub_queue = controldata
    sub_keys = "agents.*.heartbeat"
    event_prefix = control
    username = guest
    password = guest

[Logging]
    level = info
    location = control.log

[Database]
    filepath = control.db 
